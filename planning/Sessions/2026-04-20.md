---
date: 2026-04-20
---

# Session 2026-04-20

## Erledigt

### Audit-Plan fuer Struktur, Effizienz und Sicherheit erstellt

- eigenstaendige Checklisten-Datei angelegt: `planning/Audit-Plan-Code-Struktur-Effizienz-Sicherheit.md`
- empfohlene Agency-Agents fuer das Projekt priorisiert:
  - `Codebase Onboarding Engineer`
  - `Software Architect`
  - `Backend Architect`
  - `Frontend Developer`
  - `Database Optimizer`
  - `Security Engineer`
  - `Code Reviewer`
  - `Performance Benchmarker`
- lokale Skills fuer die praktische Durchfuehrung im Repo zugeordnet:
  - `code-review`, `security-review`, `semgrep`, `codeql`, `gha-security-review`, `web-perf`, `webapp-testing`
- Plan bewusst in einzelne Sessions mit Session-Prompts, Checklisten und erwarteten Artefakten zerlegt

### Admin-Backend: Security- und Infrastruktur-Härtung

- `requireAuth` auf `/admin/social-cards` ergänzt; Seite ist jetzt wie die zugehörigen Daten-Endpunkte geschützt
- Legacy-Auth über `X-Admin-Token` entfernt; Admin-Auth läuft jetzt nur noch über das `httpOnly`-Cookie
- `requireAuth` setzt `req.adminSessionId`, damit Audit-Logs weiterhin eine Session-Kennung erhalten
- `adminError()` bereinigt weiterhin Fehlermeldungen, sendet aber keine Stack-Traces mehr an den Client
- Login-Härtung: `timingSafeEqual` bei unterschiedlich langen Admin-Keys erzeugt kein unterscheidbares Verhalten mehr
- Backup-Restore verlangt jetzt explizit `confirm: true`; Admin-Frontend sendet das Bestätigungsfeld mit

### Admin-Backend: Validierung & Performance

- Fehlende Zod-Validierung für mehrere Admin-Endpunkte nachgezogen:
  - `/admin/stats`
  - `/admin/stats/summary`
  - `/admin/stats/export`
  - `/admin/audit-log`
  - `/admin/social-cards/tagesdata`
  - `/admin/social-cards/belege`
- `POST /admin/tag` schreibt Lemmata jetzt direkt per SQLite-Upsert statt per Full-Load + `findIndex()` + Full-Save
- `stmts` und `lemmaToRow()` aus `server/store.js` exportiert, damit die Route die vorhandenen DB-Primitives direkt nutzen kann
- nicht mehr verwendete Hilfsfunktion `normalizeDays()` aus `server/routes/admin.js` entfernt

### Admin-Backend: Audit-Log auf SQLite migriert

- neue Tabelle `audit_log` in `server/db.js` angelegt
- Indizes für Zeitstempel, `(resource, resource_id)` und `(action, status)` ergänzt
- `server/audit.js` vollständig auf SQLite umgestellt
- bestehende Legacy-Datei `server/data/audit.log` wird beim Start einmalig und idempotent importiert
- neue Audit-Einträge werden nur noch in SQLite gespeichert
- bestehende API (`getAuditLog`, `getAuditLogByResource`) bleibt kompatibel, Admin-Dashboard musste dafür nicht umgebaut werden

### Klassenraum-Tab: Socket-Persistenz & State-Sync (Phase 1)

- `ClassroomTab` permanent gemountet (außerhalb `TabTransition`, `display: none` statt Unmount)
  — verhindert, dass beim Tab-Wechsel ein neuer Teilnehmer angelegt wird
- `classroom:state`-Handler korrigiert: aktualisiert jetzt `participantSession`-State
  (zuvor leeres Handler-Stub → „Warte auf Start" blieb stehen auch wenn Session lief)
- `isLive`-Berechnung korrigiert: blinkt nur bei `running + socket`, nicht schon beim Verbinden
- Auto-Restore-Guard: beendete/archivierte Sessions werden aus `localStorage` entfernt und
  nicht wiederhergestellt
- `localStorage`-Cleanup wenn Server `finished`/`archived` per Socket meldet
- `app--tab-klassenraum`-Klasse in `App.jsx` → CSS-Regel kollabiert `tab-transition-container`
  auf `height: 0` (behebt großen Leerraum über Klassenraum-Inhalt)

### Klassenraum-Tab: Layout & UI (Phase 2)

- Statische Raster-Wörter `①②③④` entfernt, durch dynamische Statuszeile ersetzt:
  - Lehrkraft: Sitzungsname · Status · Verbunden-Zahl · Abgaben-Zähler
  - Schüler: Sitzungsname · Verbunden/Läuft/Warte auf Start · abgegebene Spiele
  - Ohne Account: „Code unter ② eingeben"
- `①②③④`-Folio-Nummern im Raster entfernt
- „Verbunden"-Bug behoben: Schüler-Section zeigt jetzt entweder Join-Formular ODER
  Verbunden-Status (nie beides gleichzeitig)
- „Anderen Code eingeben"-Button im Verbunden-Status ergänzt (setzt Session/Socket zurück)
- ③ Abgaben-Section: zustandsabhängige Texte (Lobby / Beendet / Leer / Abgaben-Liste)

### Klassenraum-Tab: Timer & Auto-Submit (Phase 3)

- **Timer-Fix**: 5s-Dashboard-Poll-Interval startet nicht mehr für `finished`/`archived`
  Sessions — Guard am Anfang des `useEffect` verhindert `setInterval`
- **Auto-Submit-Fix**: `pendingSubmitsRef` puffert Spielergebnisse die vor Session-Start
  eingehen (Lobby-Phase); `participantSessionRef` hält aktuellen Session-State für Closures;
  beim `classroom:state(running)`-Event werden alle gepufferten Abgaben sofort nachgereicht
- Beide Refs (`pendingSubmitsRef`, `participantSessionRef`) ergänzt

### Klassenraum-Tab: Retro-Submit & Reconnect-Guard (Phase 4)

- **③ Retro-Submit**: Schüler, die erst nach Sitzungsstart beitreten (oder wieder verbinden),
  übertragen ihre bereits gespielten Ergebnisse automatisch.
  - `getRetroResultsRef` in `App.jsx`: hält aktuelle Funktion die `playedGames`, `zrPlayed`,
    `wzPlayed`, `zwPlayed` aus localStorage liest und als `{game, score, maxScore}[]` zurückgibt
  - `isFreshJoinRef` in `ClassroomTab`: markiert echten Beitritt/Reconnect
  - Im `socket.on('connect')`-Handler: bei gesetztem Flag → Retro-Ergebnisse in `pendingSubmitsRef`
    einstellen + optimistisches UI-Update in `submittedGames`
  - Im `classroom:ready`-Handler: Pending-Flush wenn Session bereits `running`
    (ergänzend zum bestehenden Flush in `classroom:state`)
- **④ Reconnect-Guard**: verhindert, dass ein Schüler bei erneutem Beitritt als neuer
  Teilnehmer gezählt wird.
  - Beim Beitritt: Code → `sessionStorage` (`sig_cr_${code}`) mit `{sessionId, participantId, token, session}`
  - Beim erneuten Beitreten mit gleichem Code: HTTP-Join übersprungen, gespeicherte Credentials
    direkt für Socket-Reconnect verwendet → Teilnehmerzahl bleibt korrekt
- Bei beendeter/archivierter Session: sessionStorage-Eintrag wird bereinigt, dann normaler Join
- „Sitzung verlassen": localStorage entfernt (Auto-Restore verhindert), sessionStorage **bleibt**
  (gleicher Code reconnected als selber Teilnehmer)

### Admin-Portal: UX-, A11y- und Restore-Fixes

- bestätigte Review-Punkte aus dem Admin-Portal umgesetzt (High bis Low)
- `/admin/tag` ersetzt optionale Spielmodi jetzt vollständig:
  - leere `zeitreise`-, `wortzwilling`- oder `zeitenwende`-Felder löschen vorhandene Daten für den Tag
- Kalenderzellen im Admin sind jetzt echte Buttons statt klickbarer `div`s
  - dadurch per Tastatur erreichbar und besser mit Screenreadern nutzbar
- Admin-Frontend von Inline-Handlern auf delegierte Event-Listener umgestellt
  - betrifft `server/admin.html` und dynamisch gerenderte Elemente in `server/public/admin.js`
- CSP für `/admin` verschärft:
  - `script-src 'unsafe-inline'` entfernt
  - `style-src 'unsafe-inline'` bleibt weiterhin nötig wegen dynamischer Inline-Styles in Visualisierungen
- automatische Session-Erneuerung im Admin-Client ergänzt (`/admin/refresh`), damit 8h-Sessions nicht kommentarlos auslaufen
- Nutzer-Suche im Admin verbessert:
  - Debounce eingebaut
  - laufende Requests werden bei neuer Suche abgebrochen
- Rollen-Select im Nutzerdetail springt bei Save-Fehlern wieder auf den vorherigen Zustand zurück
- „Heute"-/Datumslogik in `server/public/admin.js` auf dynamische `Date`-Berechnung umgestellt
  - keine beim Skriptstart eingefrorene Zeit mehr
- kleine Mobile-/Touch-Verbesserungen:
  - mehrere Buttons und Controls auf `min-height: 44px` angehoben
- Tabellen-Fix:
  - falsches `colspan` im Nutzer-Ladezustand von `5` auf `6` korrigiert

### Admin-Backend / Store: Atomischer Restore

- Backup-Restore auf atomischen Gesamt-Write umgestellt
  - neue `replaceAllAdminData(...)`-Schreibroute in `server/store.js`
  - Kalender, Zeitreise, Wort-Zwilling, Zeitenwende, Stats und Lemmata werden in einer gemeinsamen SQLite-Transaktion ersetzt
- `save('lemmata.json')` ersetzt die Lemma-Tabelle jetzt vollständig statt nur Upserts zu fahren
  - wichtig für konsistente Restore-Snapshots
- Restore-Validierung für kaputte `stats-rows` ergänzt
  - ungültige Backup-Zeilen brechen die Transaktion sauber ab

### Admin-Tests

- `server/__tests__/admin.routes.test.js` erweitert um Regressionstests für:
  - Löschen optionaler Modi beim erneuten Speichern eines Tags
  - atomisches Verhalten von `/admin/backup/restore` bei Fehlern
  - vorhandene Tagesvorschau robuster gegen leeren Kalenderzustand gemacht

## Verifikation

- `npm run build` erfolgreich nach allen Änderungen
- `npx vitest run server/__tests__/admin.routes.test.js server/middleware/auth.test.js` erfolgreich (`32/32` Tests grün)
- `npm.cmd run test -- server/__tests__/admin.routes.test.js` erfolgreich (`15/15` Tests grün) nach den Admin-Portal-Fixes
- `npm.cmd run lint` **nicht möglich**, da aktuell kein `lint`-Script in `package.json` existiert

## Commits

- `e8c4dbc` Klassenraum-Fixes Phase 1 + 2 (aus vorheriger Context-Window-Session)
- `52c9598` fix(classroom): stop polling after session ends, cache pre-start submissions
- `880247e` fix(classroom): retro-submit on late join, sessionStorage reconnect guard
- `ff6f634` fix(admin): harden admin backend and migrate audit log
- `533e89b` fix(admin): tighten portal UX and restore safety

## Offen / Nächste Session

- „0 von 3 abgegeben" bei alten Test-Sessions: Artefakt aus der Zeit vor dem Always-Mount-Fix
  (mehrfach angelegte Teilnehmer) — für neue Sessions kein Problem
- Klassenraum-Tab bisher nur lokal getestet; Integrationstest mit echter Schüler-Gruppe ausstehend
- Optionales: Export-Polling nach Session-Ende (aktuell manuell via Refresh)
- Raster-Header: weitere Titelleiste-Anpassungen noch offen

### Audit Session 1: Repo-Kartierung / Audit-Setup

- Hauptmodule fuer das spaetere Audit identifiziert:
  - Frontend-Orchestrierung: `src/App.jsx`
  - grosse UI-/State-Hotspots: `src/components/ClassroomTab.jsx`, `src/components/TabPlaceholders.jsx`, `src/components/Home.jsx`
  - Backend-Bootstrap: `server/index.js`
  - Public API: `server/routes/public.js`
  - Admin-Backoffice: `server/routes/admin.js`
  - Auth: `server/auth/index.js`, `server/middleware/auth.js`, `server/middleware/userAuth.js`, `server/routes/account.js`
  - Persistenz: `server/db.js`, `server/store.js`, `server/query-cache.js`
  - Classroom/Realtime: `server/routes/classroom.js`, `server/classroom-store.js`, `server/realtime/classroomSocket.js`
  - CI/CD: `.github/workflows/deploy.yml`
- wichtigste Querverbindungen kartiert:
  - `src/App.jsx` -> `src/config.js` -> `server/routes/public.js` -> `server/store.js`
  - Konto-Flow ueber Better Auth + `server/routes/account.js`
  - Admin-Flow ueber separates Custom-Admin-Session-System
  - Classroom ueber REST + Socket.IO + Export-Module
- primaere Audit-Hotspots identifiziert:
  - Struktur: `src/App.jsx`, `src/components/ClassroomTab.jsx`, `src/components/TabPlaceholders.jsx`, `server/routes/admin.js`, `server/store.js`
  - Effizienz/Performance: blockierende SQLite-Zugriffe im Ein-Prozess-Setup, Whole-table-Loads/Caches in `server/store.js`, Polling/Socket-Mix im Klassenraum, moeglich inkonsistentes PWA-API-Caching in `vite.config.js`
  - Sicherheit: zwei Auth-Systeme, leichter CSRF-Schutz fuer Admin, externe Quellen/CSP im Admin, maechteige Admin-Operationen wie Restore/Upload, direkter Deploy auf `main` ohne strikte Workflow-Gates
- empfohlene Reihenfolge fuer die naechsten vertieften Reviews:
  1. Backend-Sicherheitskante (`server/index.js`, Auth-/Rate-Limit-/User-Middleware)
  2. Persistenz und Datenmodell (`server/db.js`, `server/store.js`)
  3. Admin-Backoffice (`server/routes/admin.js`, `server/backup.js`, `server/audit.js`)
  4. Classroom/Realtime/Export
  5. Oeffentliche Spiel-API und externe Datenquellen
  6. Frontend-State-Architektur (`src/App.jsx`, Tabs, Home)
  7. Build/PWA/Deployment

### Audit Session 2: Architektur- und Strukturreview

- Ergebnisdatei angelegt: `planning/Audit-Ergebnisse/2026-04-20-session-2-architektur.md`
- zentrale Strukturfindings festgehalten:
  - `server/routes/admin.js` als God-Router mit sehr vielen fachlich unterschiedlichen Endpunkten und Hilfslogiken
  - `src/App.jsx` als ueberladener Orchestrator fuer Navigation, Spielstatus, Entitlements, Persistenz und Classroom-Submit-Bridge
  - `src/components/ClassroomTab.jsx` als stark ueberfrachtete Multi-Rollen-Komponente mit REST-, Socket-, Storage- und Export-Logik
  - `server/store.js` als SQLite-Schicht mit historischer JSON-Datei-Abstraktion, die fachliche Grenzen verschleiert
  - `src/components/TabPlaceholders.jsx` strukturell irrefuehrend benannt, obwohl dort produktive Konto-/Auth-Logik lebt
- naechster empfohlener Schritt bleibt die Backend-/Security-Kante vor groesseren Refactoring-Entscheidungen

### Audit Session 3: Backend-/Security-Kante

- Ergebnisdatei angelegt: `planning/Audit-Ergebnisse/2026-04-20-session-3-backend-security-edge.md`
- bestaetigter High-Confidence-Befund dokumentiert:
  - Teacher-WebSocket im Klassenraum kann von normalen Teilnehmern imitiert werden, weil der Socket-Join nur clientseitig gelieferte `sessionId` + `teacherUserId` vertraut
  - Folge: unautorisierter Zugriff auf teacher-only Echtzeitmetriken und Beeinflussung der Host-Praesenz-/Timeout-Logik
- zusaetzliche nachrangige Pruefpunkte notiert:
  - Better-Auth-Endpunkte ausser `sign-in/email` und `sign-up/email` sind auf Express-Ebene nicht explizit rate-limitiert
  - globaler Error-Handler gibt auf `/admin` Detailtexte auch in Produktion zurueck
- naechster sinnvoller Schritt: Persistenz- und Datenzugriffsreview (`server/db.js`, `server/store.js`, `server/query-cache.js`)

### Audit Session 4: Persistenz- und Datenzugriffsreview

- Ergebnisdatei angelegt: `planning/Audit-Ergebnisse/2026-04-20-session-4-persistenz-datenzugriff.md`
- zentrale Daten-Layer-Befunde festgehalten:
  - `store.js` laedt fuer mehrere Kernpfade komplette Tabellen in den Node-Prozess und aggregiert dort weiter
  - `stats` ist relational gespeichert, wird im Alltag aber weiterhin oft wie eine globale JSON-Gesamtstruktur behandelt
  - mehrere getrennte In-Memory-Caches (`store.js`, `query-cache.js`) erschweren Datenfrische und Invalidation
  - `SQLitePool` degradiert bei Erschoepfung unsauber auf Connection-Wiederverwendung statt klarer Backpressure
  - `init-wortprofil.js` koppelt Provisioning und Laufzeit ueber Shell-Kommandos und Dateigroessen-Heuristik
- positive Punkte ebenfalls vermerkt:
  - sinnvolle Indizes in `db.js`
  - atomare `recordStat()`-Transaktion
  - read-only Nutzung der Belege-DB
  - sauberere Abfragekapselung in `wortprofil.js` als im Legacy-Store
- naechster sinnvoller Schritt: Frontend- und Laufzeit-Performance-Review

### Audit Session 5: Frontend- und Laufzeit-Review

- Ergebnisdatei angelegt: `planning/Audit-Ergebnisse/2026-04-20-session-5-frontend-runtime.md`
- zentrale Frontend-/Runtime-Befunde festgehalten:
  - `App.jsx` erzeugt beim Start einen unnoetigen Request-Wasserfall fuer Entitlements und Premium-Modi
  - `zeitenwende` behandelt 5xx-Antworten wie „kein Eintrag heute" und maskiert damit Backend-Stoerungen
  - PWA-Runtime-Caching in `vite.config.js` zielt auf `/api/...`, waehrend das Frontend ueber `/api/v1` laeuft
  - `TabTransition.jsx` rendert waehrend des Wechsels zweimal denselben Tree statt alte und neue Tab-Inhalte sauber zu trennen
  - `ClassroomTab.jsx` baut fuer Lehrkraefte wiederholt Kurzzeit-Sockets als Live-Bootstrap auf
- kleinere Hygiene-Kante notiert:
  - `src/utils/storage.js` nutzt weiterhin `console.error(...)` fuer Storage-Fehler
- naechster sinnvoller Schritt: CI/CD-, Deployment- und Betriebsreview

### Audit Session 6: CI/CD-, Deployment- und Betriebsreview

- Ergebnisdatei angelegt: `planning/Audit-Ergebnisse/2026-04-20-session-6-cicd-betrieb.md`
- zentrale Deploy-/Betriebsbefunde festgehalten:
  - `.github/workflows/deploy.yml` deployed direkt auf `main`, ohne explizite Test-/Build-Gates vor dem Rollout
  - Produktions-Checkout auf dem Server wird per `git reset --hard origin/main` hart auf den Remote-Stand gesetzt
  - Workflow nutzt `npm install` statt reproduzierbarem `npm ci`
  - es fehlen `concurrency`-/`environment`-Schutzschichten fuer Produktionsdeploys
  - `server/index.js` koppelt HTTP-Server, Export-Worker und Retention-Job an denselben Prozess
- zusaetzliche Betriebskante notiert:
  - Socket-CORS ist offener als die uebrige HTTP-Origin-Politik
- naechster sinnvoller Schritt: Audit-Synthese und Priorisierung der Findings aus Session 2 bis 6

### Audit-Synthese und Priorisierung

- konsolidierte Synthese angelegt: `planning/Audit-Ergebnisse/2026-04-20-audit-synthese.md`
- Findings aus Session 2 bis 6 in Prioritaetsstufen gebuendelt:
  - `P0`: Teacher-Socket-Autorisierung, Deploy-Gates
  - `P1`: Admin-/Store-Entflechtung, Frontend-Bootstrap, Deploy-Verfahren
  - `P2`: groessere Architektur- und Runtime-Aufraeumarbeiten
  - `P3`: Betriebs- und Hygiene-Themen
- empfohlene Umsetzungswellen und konkrete Arbeitspakete festgelegt:
  - Paket A: Klassenraum-Sicherheit haerten
  - Paket B: Deploy absichern
  - Paket C: Store/Admin entflechten
  - Paket D: Frontend-Bootstrap reduzieren
- naechster sinnvoller Schritt: Umsetzung ab Paket A oder auf Wunsch in einzelne Tickets/PR-Pakete zerlegen

### Audit Session 7: Messbare Performance-Pruefung

- Ergebnisdatei angelegt: `planning/Audit-Ergebnisse/2026-04-20-session-7-performance.md`
- plan-konforme Messpunkte erhoben:
  - Production-Build erfolgreich in ca. `1.16s`
  - PWA-Precache: `43 entries`, `2831.28 KiB`
  - groesster JS-Chunk: `264.43 kB` raw / `78.54 kB` gzip
  - Haupt-CSS: `87.07 kB` raw / `15.37 kB` gzip
  - lokale Basis-Endpoints im Bereich von ca. `6` bis `11 ms` Durchschnitt
- wichtigste messbare Bottlenecks festgehalten:
  - zu schwerer PWA-Precache, dominiert von grossen Bildern
  - Hauptchunk bleibt der dominante kritische JS-Block
  - wahrgenommene Performance wird eher durch Request-Muster als rohe Serverlatenz begrenzt
- Restluecke explizit benannt:
  - keine vollstaendige Core-Web-Vitals-Messung in dieser Session
- naechster plan-konformer Schritt: Session 8 (CI/CD-, Deploy- und Betriebsreview)

### Audit Session 8: CI/CD-, Deploy- und Betriebsreview

- Ergebnisdatei angelegt: `planning/Audit-Ergebnisse/2026-04-20-session-8-cicd-ops.md`
- plan-konforme Review-Flaechen bearbeitet:
  - GitHub Actions Deploy-Workflow
  - Logging-/Health-/Shutdown-Bausteine
  - Betriebs- und Recovery-Annahmen im Repo
- zentrale Befunde festgehalten:
  - Deploy ohne vorgeschaltete Verify-Gates
  - hartes `git reset --hard origin/main` auf dem Zielserver
  - `npm install` statt `npm ci`
  - keine `concurrency`-/`environment`-Schutzschicht
  - Beobachtbarkeit nur grundlegend vorhanden, aber nicht als versioniertes Betriebs-Setup modelliert
  - Webserver und Hintergrundjobs laufen im selben Prozess
- zusaetzliche Betriebsnotiz:
  - keine PM2-Konfiguration im Repo versioniert gefunden
- naechster plan-konformer Schritt: Session 9 (Synthese und Umsetzungsplan)

### Audit Session 9: Synthese und Umsetzungsplan

- Ergebnisdatei angelegt: `planning/Audit-Ergebnisse/2026-04-20-session-9-synthese-und-plan.md`
- alle Audit-Befunde in das im Plan geforderte Format ueberfuehrt:
  - Sofortmassnahmen diese Woche
  - naechste Refactorings
  - laengerfristige Architekturthemen
  - akzeptierte Restrisiken
- zusaetzlich dokumentiert:
  - Masterliste der Findings
  - Roadmap mit Reihenfolge
  - Trennung nach Fix, Refactor, Monitoring, Test-Ausbau
- priorisierte Gesamtbewertung bestaetigt als naechste Umsetzungsreihenfolge:
  - Klassenraum-Sicherheit haerten
  - Deploy absichern
  - Store/Admin entflechten
  - Frontend-Bootstrap und Runtime vereinfachen

### Audit-Konsolidierung: plan-konformer Endstand

- Gesamtzusammenfassung angelegt: `planning/Audit-Ergebnisse/2026-04-20-audit-gesamtzusammenfassung.md`
- `planning/Audit-Ergebnisse/README.md` auf kanonischen Audit-Stand umgestellt
- Session-9-Synthese an die plan-konformen Sessions angepasst:
  - Session 3 = Backend-Review
  - Session 6 = Security-Audit
  - Session 7 = messbare Performance
  - Session 8 = CI/CD-/Ops-Review
- historische Vorarbeiten bleiben erhalten, sind aber jetzt explizit als Vorarbeit markiert
- verbleibende Inkonsistenz dokumentiert:
  - Frontend- und Daten-Layer-Review sind inhaltlich vollstaendig, liegen aber unter Session 4 und 5 historisch vertauscht vor
- Audit damit ausreichend konsistent, um die Implementierungspakete zu starten

## Branch / Kontext

- Direkt auf `main` gearbeitet
- Alle Commits auf `origin/main` gepusht
- Fokus: Klassenraum-Socket-Stabilität sowie Admin-Portal-Härtung und UX-Fixes
