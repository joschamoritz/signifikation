# Wortprofil Pipeline – Python-Abhängigkeiten
# Virtualenv mit Python 3.12 erstellen, dann: pip install -r requirements.txt
#
# PyTorch (CUDA 12.6) – separat installieren, BEVOR diese Datei ausgeführt wird:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
#
# de_zdl_lg – separat installieren:
#   pip install de-zdl-lg --index-url https://gitup.uni-potsdam.de/api/v4/projects/21461/packages/pypi/simple
#
# wordprofile – separat installieren:
#   pip install git+https://github.com/zentrum-lexikographie/wordprofile.git

# NLP
spacy>=3.7,<4.0
dwdsmor

# Korpus-Verarbeitung
wikiextractor
tqdm

# Daten
lxml
