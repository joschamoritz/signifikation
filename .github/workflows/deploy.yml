          script: |
            export NVM_DIR="$HOME/.nvm"
            source "$NVM_DIR/nvm.sh"
            cd /root/app
            git pull
            npm install --omit=dev
            npm run build
            pm2 restart signifikation
